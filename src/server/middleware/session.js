import axios from 'axios';
import { AUTH_ENDPOINT_BACK } from 'Configs';
import logger from 'Utils/logger.js';


export const sessionLogin = async(req, res, next) => {
  next();
};

export const session = async(req, res, next) => {
  next();
};
